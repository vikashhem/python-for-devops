text = "   Some spaces around   "
stripped_text = text.strip()
print("Stripped text:", stripped_text)

#by default it will only strip the white space 
#if you want to strip anything particular try .strip("value that you want to strip")
#there is lstrip() rstrip() for left side strip & right side strip 
